"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.sshShell = void 0;

var _ssh2shell = _interopRequireDefault(require("ssh2shell"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var sshShell = function sshShell(config, cb) {
  var host = {
    server: {
      host: config.host,
      port: config.port,
      userName: config.user,
      password: config.password,
      privateKey: config.privateKey
    },
    commands: config.commands
  };
  var SSH = new _ssh2shell["default"](host);
  SSH.connect(function (output) {
    cb(output);
  });
};

exports.sshShell = sshShell;